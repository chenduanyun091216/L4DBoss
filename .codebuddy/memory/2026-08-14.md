# 2026-08-14

## 晴空白主题（light）UI 优化 (l4d2_mod_manager/app.py)
用户反馈亮色主题下分割线/状态色在白底上太浅、看不清，做了三批调整：

1. **分割线/边框加深**：主分割线（header/sidebar/footer/dialogHeader/状态栏/详情/文件对话框）由 `#d3dbe8` → `#b3c0d4`；输入框/按钮/卡片/菜单/提示框边框由 `#c3cfe0` → `#aab6c8`；卡片边框 `#aeb9c9` → `#9aa7ba`；菜单分隔符 `#e0e6f0` → `#c6d0de`。
2. **激活/冲突卡片配色加深**：
   - 激活：背景 `rgba(201,223,252,255)`，边框 `#1c4fd0`
   - 冲突：背景 `rgba(250,211,216,255)`，边框 `#d8363f`，光晕加深
   - favorite 选中态边框与光晕统一为 `#d8363f`
3. **底部分割线再加深**：footer 与 QStatusBar 的 `border-top` 由 `#b3c0d4` → `#9aa7ba`。

暗色主题（dark）始终未改动。

## app.py 模块化重构（零功能/界面改动）
将 4124 行的巨型 `app.py` 拆分为多文件，保持所有类名、方法签名、运行时行为 100% 不变（方法通过 `MainWindow.xxx = xxx` 绑定，仍是实例方法）：

- `l4d2_mod_manager/theme.py`：主题常量、STYLESHEET、`ui()`、`load_font()` 等（原 1-483 行）
- `l4d2_mod_manager/components.py`：所有独立组件类（ModCard、各 Dialog、ToggleSwitch、MultiSelectComboBox 等）+ 顶层工具函数（steam_sync_candidates/fetch_steam_for_mods/make_tag/clear_layout 等）
- `l4d2_mod_manager/main_window.py`：MainWindow 类骨架（core 方法）+ 绑定其余子模块方法 + main() 入口在 `app.py`
- `main_window_build.py` / `main_window_cards.py` / `main_window_mods.py` / `main_window_collections.py` / `main_window_steam.py` / `main_window_conflicts.py` / `main_window_details.py` / `main_window_events.py`：按职责拆分 MainWindow 方法
- `app.py` 现仅 26 行入口，重新导出 main()

注意：`run.py` 的 `from l4d2_mod_manager.app import main` 不变。根目录残留临时脚本 `_split_app.py`（用户拒绝删除），后续可清理。
`tests/test_collection_sync.py::test_collection_folder_rejects_empty_or_reserved_names` 失败为预先存在（与 collection_sync.py 逻辑相关，非本次重构引入）。
